node_modules/
dist/
.env
*.local
.devcontainer
.temp/
workspaces/
planner-workspaces/

# Kiro IDE files — contain infra notes, internal tooling context, and credentials.
# Keep agents and hooks (those are part of the project), but exclude everything else.
.kiro/steering/
.kiro/skills/
